<div class="container">
    <div class="row">
        <div class="col-md-4">
            <h2 class="title-small">{{ post_about.meta('title_about') }}</h2>
            <h3 class="title-big fw-bold mb-5">{{ post_about.meta('subtitle_about') }}</h3>
            <p>{{ post_about.meta('txt_about') }}</p>
        </div>
        <div class="col-md-8">
        <div class="img-present">
           <img src="{{post_about.meta('img_about')}}" alt="present" class="img-fluid img-cover">
        </div>
        </div>
    </div>
</div>

{# {{ dump(post.meta('subtitle_about')) }} #}